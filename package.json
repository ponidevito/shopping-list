{
  "name": "shopping-list",
  "version": "1.0.0",
  "private": true,
  "description": "Offline shopping list PWA wrapped for Android with Capacitor.",
  "scripts": {
    "prepare:android": "node scripts/prepare-capacitor-assets.js",
    "android:add": "npm run prepare:android && npx cap add android",
    "android:sync": "npm run prepare:android && npx cap sync android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest"
  }
}
